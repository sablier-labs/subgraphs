type Watcher @entity {
  """
  The chain ID. There is one watcher per subgraph.
  """
  id: String!

  """
  Global index for actions.
  """
  actionIndex: BigInt!

  """
  Alias for `id`.
  """
  chainId: BigInt!

  """
  Global index for streams.
  """
  streamIndex: BigInt!
}
