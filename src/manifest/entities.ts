const flow = ["Action", "Asset", "Batch", "Batcher", "Broker", "Contract", "Stream", "Watcher"];

export const entities = {
  flow: {
    "v1.0": flow,
    "v1.1": flow,
  },
};

export default entities;
